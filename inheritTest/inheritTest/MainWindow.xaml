<Window x:Class="inheritTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:inheritTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="725">
    <Grid Margin="0,0,0,-3" HorizontalAlignment="Left" Width="715">
       

        <Button x:Name="newMobile" Content="Add new mobile" HorizontalAlignment="Left" Height="63" Margin="10,10,0,0" VerticalAlignment="Top" Width="129" Click="newMobile_Click"/>
        <TextBox x:Name="firstDataTextBox" HorizontalAlignment="Left" Height="36" Margin="0,78,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="507" />
        <Button x:Name="readDBbutton" Content="Read from DB" HorizontalAlignment="Left" Height="63" Margin="178,10,0,0" VerticalAlignment="Top" Width="129" Click="readDB_Click"/>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" Height="179" Margin="24,119,0,0" VerticalAlignment="Top" Width="493" SelectionChanged="dataGrid_SelectionChanged">
            
            <DataGrid.Columns>
                <!--<DataGridTextColumn x:Name="ID" Header ="ID" IsReadOnly="True"  Binding="{Binding mobUid}"/>
                <DataGridTextColumn x:Name="Producer" IsReadOnly="True"  Binding="{Binding mobProd}" />
                <DataGridTextColumn x:Name="Model" IsReadOnly="True" Binding="{Binding mobMod}"   />
                <DataGridTextColumn x:Name="IMEI" IsReadOnly="True" Binding="{Binding mobImei}"   />
                <DataGridTextColumn x:Name="Comments" IsReadOnly="True" Binding="{Binding mobComments}" />
                <DataGridTextColumn x:Name="Owner"  IsReadOnly="True" Binding="{Binding mobOwner}"  />
                <DataGridTextColumn x:Name="InvoiceNr" IsReadOnly="True"  Binding="{Binding mobInvoice}"  />-->
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
